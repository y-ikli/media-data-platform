# Docker only — installed on top of apache/airflow:2.9.3
# Do not add apache-airflow here (already in the base image)

# Google Cloud
google-cloud-bigquery==3.14.0
google-cloud-storage==2.11.0
apache-airflow-providers-google==10.10.1

# Advertising APIs
google-ads==22.1.0
facebook-business==18.0.2

# dbt (called via subprocess in DAGs)
dbt-core>=1.7.0
dbt-bigquery>=1.7.0

# Utilities
python-dotenv>=1.0.0