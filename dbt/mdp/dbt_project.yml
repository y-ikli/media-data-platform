name: 'mdp'
version: '1.0.0'
profile: 'mdp'

# Chemins
model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

# Variables globales
vars:
  gcp_project: media-data-platform
  raw_dataset: mdp_raw
  staging_dataset: mdp_staging
  marts_dataset: mdp_marts

# Configuration des modèles par couche
models:
  mdp:
    staging:
      +materialized: view
      +schema: mdp_staging
    intermediate:
      +materialized: view
      +schema: mdp_intermediate
    marts:
      +materialized: table
      +schema: mdp_marts
